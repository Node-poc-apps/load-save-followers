{
  "name": "load-save",
  "version": "1.0.0",
  "description": "Load file and save, Test with Request-promise",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "yojecode",
  "license": "ISC",
  "devDependencies": {
    "fs": "0.0.1-security",
    "request": "^2.79.0",
    "request-promise": "^4.1.1"
  }
}
